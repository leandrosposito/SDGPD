# AUDIT 14 — Deuda declarada

**Fecha:** 2026-09-06

## Alcance revisado (archivos/carpetas)

Todos los `docs/VERIFICACION_TANDA_*.md` (9 archivos: `0_1`, `2`, `2_5`, `3A`, `3B`, `3C`, `3D`, `3E`, `3G`), `docs/PENDIENTES.md`, `docs/DECISIONES_TECNICAS.md` (solo para confirmar fechas de tandas, no releído línea por línea), y el estado real de las Tandas 3f/3g contra el código actual.

## Hallazgos

| # | Severidad | Archivo:línea | Descripción | Impacto con volumen | Propuesta |
|---|---|---|---|---|---|
| 1 | ALTO | `docs/VERIFICACION_TANDA_3A.md:118-125`, `3B.md:115-122`, `3C.md:108-114`, `3D.md:129-137`, `3G.md:90-95` | 5 tandas completas (3a Pedidos, 3b Caja, 3c Settings, 3d Clientes/Directorio, 3g Movimientos+Historial) están cerradas **a nivel de código** (commiteadas, `tsc`/`build`/`lint` limpios) pero con el checklist de verificación en navegador **100% sin ejecutar** — ningún punto de ninguna de las 5 se confirmó en los hechos, incluidos los puntos "centrales" de cada tanda (ej. 3a/3b punto 4: "¿el registro creado sobrevive a navegar afuera y volver?", que es exactamente el bug que esas tandas dicen resolver). | Si el navegador revela que alguna mutación SÍ se sigue perdiendo, o que el filtro por sucursal de 3g no funciona en la práctica, el hallazgo se descubre recién cuando ya hay 5 tandas más construidas encima, no en el momento en que se introdujo. | Priorizar la corrida de estos 5 checklists antes de avanzar a Fase C — son mecánicos, no requieren leer código, y ya están escritos punto por punto. |
| 2 | MEDIO | `docs/VERIFICACION_TANDA_3E.md:194-205` | Tanda 3e (Stock Actual + Bajo Stock Mínimo con búsqueda/orden) tiene **un solo punto verificado de 12** (A3: "Stock Bajo" coincide con Bajo Stock Mínimo) — el resto, incluidos los 3 puntos nuevos de búsqueda/orden server-side agregados en esa misma tanda (D1-D3), sin ejecutar. | Búsqueda/orden de Bajo Stock Mínimo es funcionalidad nueva sin ningún tipo de confirmación funcional todavía. | Mismo tratamiento que el hallazgo 1. |
| 3 | MEDIO | `docs/VERIFICACION_TANDA_2.md:140-148` | La tabla de resultados de Tanda 2 (cache/dedupe/invalidación de TanStack Query en los 7 listados paginados) tiene los 7 puntos con la celda de resultado **vacía** — a diferencia del resto de los documentos, ni siquiera dice "No ejecutado" explícitamente, simplemente no se llenó. Funcionalmente equivalente a "no verificado", pero es una inconsistencia de formato que vale la pena señalar porque hace más fácil pasarlo por alto en una revisión rápida del archivo. | Ninguno directo — es un hallazgo de higiene documental, no de código. | Completar la tabla (aunque sea con "No ejecutado" explícito) para que sea consistente con el resto de los `VERIFICACION_TANDA_*.md`. |
| 4 | BAJO | `docs/VERIFICACION_TANDA_2_5.md:136-142` | Tanda 2.5 (httpClient unificado + `useCachedQuery`) tiene 2 de 7 puntos verificados (1: segunda carga de Inventario instantánea; 4: invalidación cruzada Inventario→Pedidos) — son justamente los dos que motivaron la tanda, así que lo esencial sí se confirmó, pero el resto (conteo exacto de `start` en consola, que TODOS los servicios ya pasen por `httpClient`, invalidación selectiva por sucursal vs. barrido total, `VITE_MOCK_LATENCY_MS` afectando a todos los módulos) sigue sin confirmar. | Bajo — lo crítico de esta tanda sí está confirmado; lo pendiente es detalle de comportamiento fino. | Completar si hay tiempo, no bloqueante. |
| 5 | ALTO | (no hay archivo) | **Tanda 3f (Reposición) nunca se empezó.** Confirmado explícitamente en `docs/VERIFICACION_TANDA_3G.md:11-12`: *"Fuera de esta tanda, sin tocar: Reposición (`TabPurchases`) — esa es la Tanda 3f"*. `TabPurchases.tsx` sigue leyendo `INVENTORY_MOCK_DATA.suggestions` filtrado en el cliente por `activeBranchId` (ver `docs/AUDITORIA_ESCALABILIDAD.md` B3/A2), sin capa `api/`, sin `usePagedQuery`, y sin que `PurchaseSuggestion` haya sido tocado por ninguna tanda reciente. El prompt maestro la lista como "pendiente previa" junto a 3g — 3g ya está cerrada a nivel de código (hallazgo 8 más abajo), 3f sigue completamente intacta. | Cualquier tanda de Fase C que asuma que Reposición ya tiene su capa `api/` (ninguna la asume hoy, pero conviene dejarlo explícito) partiría de una premisa falsa. | Confirmar con Leandro si 3f entra en el alcance de esta ronda de trabajo o queda para después de Fase A/B. |
| 6 | BAJO | `docs/PENDIENTES.md:12-26` (ítem 1) | Falta el botón "Editar" en la UI de Proveedores — la lógica de edición ya existe completa en `SupplierFormModal`/`SuppliersPage`, solo falta el disparador. Vigente, sin cambios desde que se detectó. | Ninguno con volumen — es una funcionalidad faltante, no un problema de escala. | Agregar el botón cuando se decida cerrar este ítem (no es parte de ninguna tanda de Fase C del prompt maestro actual). |
| 7 | BAJO | `docs/PENDIENTES.md:207-221` (ítem 13) | `updateProduct` descarta los lotes (`ProductLot[]`) del producto al editar — bug preexistente, confirmado y preservado sin cambios en Tanda 3e. Vigente. | Con catálogos grandes y edición frecuente de productos, cada edición borra silenciosamente el historial de lotes cargados — dato de negocio perdido, no solo un problema de escala. | Corregir cuando se decida (conservar `lots` del registro anterior salvo que el formulario los edite explícitamente). |
| 8 | INFO | `docs/PENDIENTES.md:192-205` (ítem 12) | Tanda 3g (branchId en `InventoryMovement`/`ProductHistoryEvent`) está cerrada a nivel de código, con un script de verificación contra los datos crudos del mock (confirma conjuntos disjuntos por sucursal) — pero, igual que el resto de las tandas recientes, sin verificación en navegador. Ya contemplado en el hallazgo 1 de esta tabla; se deja como entrada separada porque es la tanda más reciente y la que más directamente habilita el trabajo de Fase C (Tanda 4, selector de sucursal). | Igual que hallazgo 1. | Igual que hallazgo 1 — priorizar su checklist (`VERIFICACION_TANDA_3G.md`) en particular, dado que Fase C depende de que el filtrado por sucursal de estas dos tabs sea correcto en los hechos, no solo por script. |
| 9 | BAJO | `docs/PENDIENTES.md:154-177` (ítem 10) | 4 tabs de Inventory (Ajustes de Stock, Categorías, Listas de Precios, Importar/Exportar) siguen siendo UI construida sin funcionalidad real (sin `onClick`, datos hardcodeados no conectados a ningún tipo de `shared/types/`). Documentado explícitamente como features futuras, no código muerto — no se debe borrar. Confirmado que sigue vigente (ninguna tanda posterior a 3e las tocó). | Ninguno hoy — no hay datos reales detrás que escalar. | Ninguna acción — es un recordatorio para no confundirlas con deuda a limpiar. |
| 10 | BAJO | `docs/PENDIENTES.md:179-190` (ítem 11) | `StockAdjustmentModal.tsx` sigue huérfano (no montado en ningún lado). Vigente. | Ninguno — código sin punto de montaje no consume ciclos de usuario real, solo mantenimiento. | Ninguna acción salvo que se decida implementar un flujo real de ajuste manual de stock. |

Severidad: BLOQUEANTE / ALTO / MEDIO / BAJO (se agregó también INFO para el ítem 8, que es una nota de contexto más que un hallazgo nuevo distinto del 1)

## Qué está bien (para no romperlo después)

- La disciplina de dejar un `docs/VERIFICACION_TANDA_<n>.md` por cada tanda, con puntos concretos y accionables ("qué deberías ver" / "si no pasa esto"), es sistemática y no se abandonó en ninguna tanda — incluso las tandas con 0 puntos verificados tienen el checklist completo y listo para ejecutar, no hay ningún caso de "tanda sin ningún documento de verificación".
- Cuando SÍ se ejecutó una verificación (3e punto A3, 2.5 puntos 1 y 4), quedó registrado con detalle de qué se confirmó exactamente y con qué matiz (ej. 3e A3 aclara qué sub-chequeo no se registró por separado) — no son check marks vacíos, tienen contenido real.
- `docs/PENDIENTES.md` distingue explícitamente entre deuda vigente, deuda que no reproduce (con la investigación que lo confirma), y deuda que es feature futura no código muerto — evita que una limpieza futura borre por error algo que fue una decisión deliberada.

## Preguntas abiertas para Leandro

1. De las 5 tandas con checklist 100% sin ejecutar (hallazgo 1) más 3e (hallazgo 2), ¿se corren todas antes de empezar Fase C, o se acepta el riesgo y se sigue avanzando con la deuda de verificación acumulándose? El prompt maestro no lo resuelve explícitamente para esta situación (habla de verificación por tanda futura, no de un backlog ya acumulado de tandas pasadas).
2. ¿Tanda 3f (Reposición) entra en el alcance de esta ronda de auditoría/implementación, o se trata como trabajo aparte que se retoma después de Fase A/B? Ninguna auditoría de esta ronda (A1-A13) la trató como "ya migrada" porque no lo está, pero conviene una decisión explícita antes de Tanda 4 (selector de sucursal), que según el prompt maestro "habilita" justamente terminar 3f.
